package com.example.homegym.data.exercises

enum class ExerciseType {
    CountAmount, CountAmountTwoSide, CountDuration, CountDurationTwoSide
}

enum class ExerciseLevel {
    Easy, Medium, Hard
}

enum class BodyPartBestFor {
    Chest, Abs, Arm, Shoulder, Leg, Warmup, Stretch
}